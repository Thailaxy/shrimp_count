services:
  - type: web
    name: shrimp-count-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k uvicorn.workers.UvicornWorker api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
